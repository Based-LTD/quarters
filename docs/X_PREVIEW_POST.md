# X preview post — devnet public preview

Rules (from the playbook): media by default, one bold opinion + proof, no
engagement-bait closer, ≤2 posts/day, be in the replies for 30 min.

## Post 1 (the opener) — attach: 15s screen capture of the floor → a cabinet → a run → ON THE BOARD

QUARTERS is open on devnet.

24 arcade games. A quarter a play. Every high score is a replay anyone can re-run. Every pot pays out on-chain.

Free to play right now with test SOL. Break it before real money does.

quarters.fun

## Post 2 (the proof, next day) — attach: terminal capture of `node tools/replay.js <receipt>` printing REPRODUCED

The part that matters: nobody has to trust our leaderboard.

Every score is an input recording. The verifier replays it against a seed committed on-chain before you played. Then it publishes the receipt.

One command re-runs any receipt on your machine. Code's public.

github.com/Based-LTD/quarters

## Reply-thread ammo (keep in the replies, not the post)
- "Is it gambling?" → No chance anywhere in a score. Same seed + same inputs = same result, forever. Skill contest with an entry fee.
- "How do you make money?" → 15% of every quarter. That's it. No token. Anything claiming to be a QUARTERS token is fake.
- "Bots?" → Input-timing analysis flags machine-regular play on the public receipt. Flagged runs can be held from payout. And the engines are public, so build a bot and show us.
- "Why devnet?" → Money route's proven, we're shaking out UX. Mainnet soft-launch with small caps follows.

## Do NOT post yet
- The Bounty "10 SOL" line until the seed amount is final.
- Anything about deeds being for sale.
